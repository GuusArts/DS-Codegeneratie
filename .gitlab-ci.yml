include:
      - project: 'ocs/cicd/parent'
        ref: parent-1.1
        file: '/.gitlab-ci.yml'

default:
      image: maven:3.8-openjdk-17-slim

.testphase:
  rules:
    - when: never
stop test:
  rules:
    - when: never

.deploy:
  rules:
    - when: never

.golive:
  rules:
    - when: never

